<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body class="bg-zinc-900">
    <div
      class="container mx-auto w-full min-h-screen bg-zinc-900 p-10 text-white justify-center"
    >
      <div class="w-full flex justify-end mr-10">
        <a
          href="/logout"
          class="bg-red-600 px-3 py-2 hover:bg-red-500 cursor-pointer rounded-md"
          >Logout</a
        >
      </div>
      <h1 class="text-3xl">Hello, <%= user.name %>👋</h1>
      <h5 class="mb-5">you can create a post</h5>
      <form action="/post" method="post">
        <textarea
          name="content"
          placeholder="What's is in you mind? "
          class="bg-transparent border-2 border-zinc-800 rounded-md px-3 py-2 resize-none w-1/3 block"
        ></textarea>
        <input
          type="submit"
          value="Create post"
          class="px-3 py-2 w-40 bg-blue-500 hover:bg-blue-400 rounded-md cursor-pointer block mt-3"
        />
      </form>
      <h3 class="my-5">Your posts</h3>
      <div class="posts">
        <% user.posts.reverse().forEach(function(post){ %>
        <div
          class="postContainer bg-zinc-800 mb-4 p-4 rounded-md border border-zinc-700 w-1/3"
        >
          <h1 class="text-blue-400 font-bold">@<%= user.username %></h1>
          <p class="text-zinc-400"><%= post.content %></p>
          <div class="flex gap-4 mt-2">
            <a href="" class="text-blue-400 mt-4">Like</a>
            <a href="" class="text-zinc-400 mt-4">Edit</a>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
    <script src="/javascripts/index.js"></script>
  </body>
</html>
